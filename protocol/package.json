{
  "name": "@qr-data-bridge/protocol",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "main": "dist/protocol.js",
  "module": "dist/protocol.js",
  "types": "src/index.ts",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
